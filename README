sudo aptitude update
sudo aptitude install libgl-dev libglu-dev libpng-dev libxcomposite-dev libxdamage-dev libxrender-dev make cmake g++ libncurses-dev  git-core xclip pkg-config #mplayer
git clone  git://github.com/koo5/lemon.git
cd lemon/lemon2
make
./l


#optional
#sudo aptitude install <xmms/audacious>-dev
#cd ../spectrum-analyzer/<xmms/audacious>plugin
#./configure
#make 
#sudo make install
#go to xmms settings, visualizations, enable cat freq
