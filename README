sudo aptitude update
sudo aptitude install clang++-4.0 libgl-dev libglu-dev libglut3-dev libpng-dev libxcomposite-dev libxdamage-dev libxrender-dev make cmake libncurses-dev  git-core xclip pkg-config libcurl-dev libcurl libjson-dev # libboost-dbg libboost-dev  libcurl4-gnutls-dev freeglut3-dev  libsdl1.2-dev
cd lemon2
make
./l

#b4 pulling updates, discard your changes with
#git reset --hard HEAD

#optional
#sudo aptitude install <xmms/audacious>-dev
#cd ../spectrum-analyzer/<xmms/audacious>plugin
#./configure
#make 
#sudo make install
#go to xmms settings, visualizations, enable cat freq

